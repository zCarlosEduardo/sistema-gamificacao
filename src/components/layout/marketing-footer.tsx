export function MarketingFooter() {
  return (
    <footer className="px-6 py-6 border-t border-[var(--color-border)] text-center text-sm text-[var(--color-text-muted)]">
      Await © {new Date().getFullYear()} — Gamificação que faz sentido.
    </footer>
  );
}